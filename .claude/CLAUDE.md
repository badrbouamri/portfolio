# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

The Next.js app is scaffolded and live: M0–M6 are complete, M7 (Iterate) is in progress. Before writing code, check `docs/tasks.md` for the current milestone and work top-to-bottom within it; do not jump ahead to a later milestone's tasks.

**Visual redesign in progress, on `feat/refonte-visuelle`** — a full blue-night/brass editorial restyle per `BRIEF-REFONTE-PORTFOLIO.md` (repo root), proceeding phase-by-phase with an owner checkpoint after each (see the brief's §11). Phase 0 (audit) and Phase 1 (tokens, self-hosted Bodoni Moda/Inter Tight, `<Backdrop />`, `.section` rhythm, `/styleguide`) are committed on that branch as of 2026-09-02; `DESIGN.md` → "Redesign in progress" has the full token mapping, the conflicts found against the system below, and what's still deferred to later phases (motion engine, case-study meta line, confidential imagery, `/cv`). Everything in this file below — the color/typography/component sections — describes the **pre-redesign** system; check the branch and `DESIGN.md` before assuming which one currently applies to a given page.

- `docs/PRD_Portfolio_Badr_Eddine_ELBOUAMRI.md` — the full product spec. It is the source of truth for design tokens, content structure, and copy rules. Read the relevant section before building the related feature rather than relying on memory of it.
- `docs/tasks.md` — milestone checklist (M0–M7) derived from the PRD's roadmap (§13). Reflects actual progress; check items off as they're completed.

Build/lint commands: `npm run dev` (Turbopack dev server), `npm run build` (production build — also validates all MDX case study frontmatter against the Zod schema in `lib/schema.ts`, so a bad frontmatter field fails the build, not just lint), `npm run lint` (ESLint), `npm run start` (serve a production build). There is no test suite yet.

As of M7, all 5 case study categories (`methodes`, `lean`, `maintenance`, `conception`, `digital`) have at least one published case study — the `conception` gap flagged throughout M2–M6 (homepage's 3rd featured slot, the Compétences page's "Conception & Simulation" and "Sciences de l'ingénieur" evidence lines) is closed. `docs/` also holds source material for case studies (PDFs, PPTX/DOCX reports, MP4s) that is not itself shipped — only the content extracted from it into `content/{fr,en}/projets/*.mdx` and `public/` is.

## What this project is

A bilingual (FR/EN) portfolio site for a mechanical engineer targeting Moroccan industrial/automotive employers. It is a static content site, not an app with users/auth/database. The two audiences with opposing needs — HR screening in 30 seconds vs. an engineering manager reading a case study in detail — is the central design constraint driving nearly every layout decision (PRD §2, §4).

## Planned architecture (PRD §9)

- **Stack:** Next.js 15 App Router + TypeScript strict, Tailwind CSS v4 (tokens in `@theme`), next-intl for locale routing (`/fr` default, `/en`), MDX content validated with Zod at build time, Recharts for charts, `@react-three/fiber` dynamically imported for the optional 3D viewer, Resend for the contact form, Vercel Web Analytics. **No database, no auth in V1** — content lives in-repo as MDX, git is the CMS.
- **Content is the bottleneck, not code** (PRD §10) — most milestones are blocked on writing/translating case studies before the corresponding page can be wired up with real content.
- **Case studies are the core content unit.** Every one follows a fixed 12-block skeleton (PRD §6) and a Zod-validated frontmatter schema (category, kpis, tools, confidential flag, etc.). The skeleton order is intentional and must not be reordered per-project — that consistency is what makes the site skimmable.
- **Confidentiality is load-bearing, not cosmetic.** Case studies drawn from internship work (Stellantis, AIC, Nexteer) must never publish absolute costs, volumes, real part/supplier names, or screenshots of internal documents/dashboards — see the hard rules in PRD §6. Any Stellantis-sourced content additionally requires verifying the confidentiality clause in the convention de stage first (M0). When touching content files, check the `confidential` frontmatter field and the organisation's `organisationPublic` flag before rendering real names.
- **Design tokens are centrally enforced**, not ad hoc — colors, type scale, and spacing are declared as CSS variables per PRD §8.2–8.4 specifically to prevent an AI-assisted build from drifting into generic/templated styling. Reuse the tokens rather than introducing new colors or one-off spacing values.
- **Case-study header (`CaseHero`, `components/case/CaseHero.tsx`)** — as of 2026-09-01, replaces the old field-grid "Cartouche" title block (organisation/période/rôle/méthodes/confidentialité fields) with a full-width project image. Case studies with a real photo (`hero` frontmatter) show it; the rest — mostly the confidentiality-restricted internship case studies, which can never carry a real photo per the confidentiality rules below — fall back to the same technical-grid pattern used on the homepage hero (`.hero-grid-bg`), so an empty header still reads as deliberate rather than broken. This was a deliberate reversal of the Cartouche's original "signature component" status (PRD §8.6 predates this change); the PRD itself has not been updated to match. The Cartouche fields (organisation/période/rôle/méthodes/confidentialité) are no longer shown anywhere on the case-study page — don't assume they're still visible when reasoning about page content.
- **Homepage "About" section (`app/[locale]/page.tsx`)** — as of 2026-09-01, replaces the old "Preuves"/"Proof points" stat bar (rebut % / internship count / tools-deployed sentence) with a first-person background blurb (`about_eyebrow`/`about_title`/`about_p1`–`about_p5` in `messages/{en,fr}.json`). The KPI-style proof stats are no longer shown on the homepage; the Stellantis scrap-cost figure and internship history are still available via the Compétences evidence links and the Parcours timeline. Restyled same-day to an eyebrow ("À propos"/"About") + short heading ("Mon parcours"/"My background") via `Section`'s existing `eyebrow` prop, plus inline emphasis on key facts (roles, organisations, headline figures) rendered with `t.rich("about_pN", { b: ... })` and a `<strong className="font-medium text-ink">` — reuses the sitewide eyebrow convention and the prose-article `strong` weight (500; IBM Plex Sans ships no 700 file) instead of introducing a one-off font, deliberately diverging from a serif-italic reference design shown for the layout pattern only. Gotcha: next-intl's ICU parser treats an apostrophe immediately before a `<b>` tag as a literal-text escape that swallows the tag — write `<b>l'ENSET Mohammedia</b>` (apostrophe inside the tag), never `l'<b>ENSET Mohammedia</b>`.
- **Engineering Projects rows (`ProjectRow`, `components/case/ProjectRow.tsx`; hero band `ProjectsHeroBand`, `components/case/ProjectsHeroBand.tsx`)** — as of 2026-09-01, replaces `ProjectCard` (deleted) on the homepage's featured section and on `/projets`: an editorial image+text row (photo one side, alternating; serif title/subtitle, summary, CTA the other) instead of a bordered grid tile, plus a dark grid-pattern hero band with an overlapping white title card on `/projets`. This is an **explicit, scoped one-off exception** to the token-system rule below, not a system update — a nearly identical ask (clone a reference site's dark/serif/red editorial look) was already evaluated and *rejected* once before (`docs/tasks.md` M7, "Homepage redesign... scoped down after flagging the conflict"); this time the user was shown that same conflict again and confirmed twice anyway, scoped to just the Projects rows. Two literal values live outside the palette/type scale for this alone — `.font-editorial` (Georgia/Times New Roman serif) and `.text-editorial-accent` (`#b5342c` red), both in `app/globals.css` and both recorded as explicit `ignore-value` entries in `.impeccable/config.json`. Do not extend either class to any other component, and do not treat this as license to clone future reference screenshots elsewhere on the site — see DESIGN.md → "Engineering Projects rows (ProjectRow) — one-off exception".
- **Compétences page skills matrix (`SkillsToolbox`, `components/skills/SkillsToolbox.tsx`)** — as of 2026-09-01, replaces the old two-column `SkillsMatrix` (deleted) with a filterable "toolbox": rounded category tabs (`aria-pressed`, horizontally scrollable below 640px via an explicit `min-[640px]:` breakpoint — the project's `--breakpoint-sm` is 768px, not Tailwind's default 640px, so don't reach for `sm:` here) + a wrapping pill grid (icon or 2-letter monogram fallback, name, 3-dot level indicator). `lib/skills-data.ts` remains the single source of truth: each `Skill` gained `icon?: string` (filename in `/public/icons/skills/`, displayed at 20px, with a graceful `onError` monogram fallback if the file is missing) and `toolbox?: boolean` (default true; `false` on the 4 engineering-science skills — machines thermiques, transfert de chaleur, mécanique des fluides, énergies renouvelables — which don't fit the toolbox's tool/method categories and are only shown in "Preuves par domaine"'s sciences group). Clicking a category tab filters client-side and surfaces a "Voir les preuves →"/"See the evidence →" link that smooth-scrolls to the matching `id="preuve-<key>"` anchor added to each Preuves-par-domaine group (`cao`+`simulation` both point at `preuve-conception`, since that evidence block bundles CAD and simulation tools together). The rounded "pill" shape needed a new `--radius-pill` token (`app/globals.css`) — the only radius exception sitewide (everything else is `--radius-hairline`/2px or square) — logged in `.impeccable/config.json`, scoped to `.skill-pill`/toolbox filter buttons only. Icons: the owner supplied 7 vendor logos in `docs/img/` (not shipped — source material only), each cropped/downscaled to a 128px square PNG in `/public/icons/skills/` — `catia.png`, `solidworks.png`, `autocad.png` (cropped from a wordmark: the source asset paired a square "A" cube mark with "AutoCAD" text, isolated via canvas pixel-boundary detection since no image-editing CLI was available in this environment), `power-bi.png`, `excel.png`, `python.png`, `matlab.png`. ANSYS and ABAQUS have no `icon` set — the owner's source assets for both were wordmark-only logos with no isolable square mark, so both intentionally fall back to the monogram, same as every skill with no real-world logo.
- **Nav/section labels renamed to "Engineering Projects"** — as of 2026-09-01, `Header.nav_projets` and `Projets.title` (both locales) changed from "Projets"/"Projects" to "Projets d'ingénierie"/"Engineering Projects"; the homepage's `Home.projets_eyebrow` value changed the same way and is now rendered as a large serif heading (`.font-editorial`) rather than the small mono eyebrow tag other homepage sections use.
- **Dark mode** (2026-09-02) — PRD §7.8 marked this DEFER pending an explicit go-ahead; given, sitewide. The 8 core tokens (`--paper`, `--surface`, `--ink`, `--graphite`, `--steel`, `--rule`, `--accent`, `--signal`) get dark values two ways in `app/globals.css`: a `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) {...} }` block for OS-preference auto mode (no JS, no flash), and a `:root[data-theme="dark"]` block for an explicit choice, which wins over the media query. `--accent`/`--signal` are brightened (not just role-swapped) so they still clear 4.5:1 as text against the new dark paper/surface — verified by computing contrast ratios directly, same rigor as the existing light-mode fixes documented above them in the file. `ThemeToggle.tsx` (`components/layout/ThemeToggle.tsx`, in `Header.tsx` next to `LocaleSwitcher`) sets `data-theme` + persists to `localStorage("theme")`; the inline bootstrap script in `app/[locale]/layout.tsx` (same script that adds `.js-reveal`) re-applies a saved choice before first paint. No explicit "reset to auto" control exists — picking light or dark is sticky until the other is picked. Components using `--ink`/`--paper` etc. as swappable text/background tokens invert correctly with no code changes (verified: `HomeSkillsGrid`'s dark cards become light cards on dark pages, `SkillsToolbox`'s active-tab `bg-ink text-paper` correctly flips too — both already relied on ink/paper being opposite ends of the scale, which holds in both themes). One fix was needed: `OrgLogo.tsx`'s partner-logo cards now use a **fixed** light plate (`bg-white`, literal `#dce0e3`/`#626d77` for border/caption, not theme tokens) because the raster logo images have dark artwork meant for a white background and can't be auto-inverted safely — documented inline. `.text-editorial-accent` (Engineering Projects rows) also got a dark-mode-only brightened value (`#e2685d` vs `#b5342c`) for the same 4.5:1 reason.
- **Homepage "Ce que je sais faire" / "What I do" skills grid (`HomeSkillsGrid`, `components/home/HomeSkillsGrid.tsx`)** — as of 2026-09-02, replaces the plain 2-column evidence list with 01–04 dark cards (`bg-ink`, `border-graphite` → `border-steel` on hover/focus, `--radius-hairline` — deliberately *not* a new rounded-card exception, see below) and a center portrait on desktop (`lg:grid-cols-[1fr_280px_1fr]`, photo `public/images/profile-cutout.png`, a user-supplied transparent PNG cutout). Tablet/mobile drop the photo and fall back to a 2×2 / stacked grid of the same cards. Only the layout (grid, corner numbering, circular arrow control) is borrowed from a reference screenshot — colors/type/radii stay in the token system per explicit user instruction this time (contrast with the Engineering Projects rows above, where the user asked for a full visual clone). The one radius used for the circular arrow button is `rounded-pill` (999px), reusing the existing `SkillsToolbox` precedent rather than adding a new exception; cards themselves stay at the sitewide 2px hairline radius even though the reference had rounder corners — an explicit user call. `Home.competence_maintenance_evidence` (both locales) was shortened to fit the ~160-char/2-3-line card constraint (dropped "et calcul des efforts de roulage" / "and calculated rolling forces"); the other three evidence sentences already fit. `page.tsx` still builds the `competences` data array from the same `Home.competence_*` i18n keys as before — only the rendering changed, no content duplicated. New `Home.home_photo_alt` key added for the portrait's alt text. A new `.skills-photo-halo` class (app/globals.css) draws a radial glow behind the photo from the existing `--color-accent` token — no new color.
- **Demos are evidence, not nav destinations** — they live under `/fr/demos/[slug]`, linked from within case studies, never from the main header. They must never ship half-finished or with a visible loading/error state; a demo not yet ready stays behind a feature flag rather than shipping broken (PRD §11.2).
- Build order matters and is specified in PRD §9.5 — scaffold/tokens/i18n first, then layout shell, then the content pipeline + one seeded case study, then the case study template, then index/filtering, then homepage, then the remaining static pages, then contact, then SEO, then demos last.

## Conventions to follow when generating copy/content

- French is the source-of-truth locale; English is a reviewed translation, never machine-translated as-is (PRD §10.5 has known FR→EN term mappings for this domain, e.g. *gamme* → routing/process sheet, *rebut* → scrap).
- All numeric data (KPIs, dates, tool tags, figure captions) is set in monospace and French-formatted in FR (comma decimal, non-breaking space before `%`) — this is a deliberate typographic rule (PRD §8.3), not a default.
- No percentage/skill bars, no decorative sequence numbering on non-sequential content, no marketing adjectives without an evidence link — tone rules are in PRD §3.5 and §8.10.
## Important Note 
after major changes , please update this file (@CLAUDE.md). keep this file up-to-date with the project’s status.