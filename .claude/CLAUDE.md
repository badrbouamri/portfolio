# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

The Next.js app is scaffolded and live: M0–M6 are complete, M7 (Iterate) is in progress. Before writing code, check `docs/tasks.md` for the current milestone and work top-to-bottom within it; do not jump ahead to a later milestone's tasks.

**Second full visual redesign, merged to `main`** (`a27a1c4`, 2026-09-05, plus two same-day follow-up commits) — a full black-ground/red-accent, dark-only clone of a reference "data scientist" portfolio template, supplied as a 52-second video and reviewed frame-by-frame (63 stills via `ffmpeg`), superseding the blue-night/brass system below in turn. Scope was confirmed with the owner first (multi-page architecture kept, real KPI values kept — only stat-card *styling* changed, no invented percentages — dark-only since the reference has no light mode). `DESIGN.md` → "Redesign — video-reference clone (2026-09-05)" and its two "Addendum" sections have the full token table, component-by-component changes, and verification notes (lint/build clean, browser-reviewed across all routes and both locales). **Everything below this paragraph and the blue-night/brass paragraph after it — Colors/Typography/Components sections, and every bullet not re-flagged again here — now describes a superseded system, not current fact**; `git diff` against `DESIGN.md`'s new section, or the actual code, is the authoritative current state. Concretely superseded by this pass: `ThemeToggle.tsx` is **deleted** (site is dark-only, no toggle, no `data-theme`/`prefers-color-scheme` branching); `--font-display` is **Archivo Black**, not Bodoni Moda; the 9 core tokens are red/black, not blue-night/brass (see DESIGN.md table); `ProjectsHeroBand`'s `.font-editorial`/Georgia exception is **removed** (component simplified to a plain centered heading, no stock photo); `Button.tsx`'s `ghost`/`solid` variants render white-outlined/white-filled at rest (sweeping to accent red on hover), not accent-colored at rest; `OrgLogo.tsx` renders every logo in full original color with **no card at all**, on the owner's explicit call — Stellantis' navy wordmark and ENSET's black subtitle text are genuinely hard to read on black as a result, a known and accepted trade-off, not a bug (a light-plate compromise was tried and removed again on request — don't re-add it without asking); a GitHub link (`github.com/badrbouamri`) now sits alongside LinkedIn/email in `Footer.tsx`, `Hero.tsx`, and the Contact page; `.section`/`.section-tight` padding is halved from the first redesign's values (96/168px → 56/96px, 32/56px → 16/24px) after screenshots found 300px+ dead gaps between adjacent sections; the homepage hero photo (`public/images/hero-cutout.png`) is the owner's supplied AI-generated portrait, cropped to its alpha bounding box before use (the source file had a huge transparent margin that made it render tiny otherwise) — the `hasHeroPhoto()` placeholder-grid fallback in `Hero.tsx` no longer fires. **Still open:** real mobile-viewport verification (this session's `resize_window` was unreliable, same limitation the prior redesign's Phase 8 hit).

**First visual redesign, merged to `main`** (PR #1, `feat/refonte-visuelle` → `main`, merge commit `4924f85`, 2026-09-02) — a full blue-night/brass editorial restyle per `BRIEF-REFONTE-PORTFOLIO.md` (repo root), executed phase-by-phase (0–8) with an owner checkpoint after each (brief's §11), then a real Lighthouse pass that found and fixed three genuine issues (contrast, missing `fetchPriority`, a heading-order gap). `DESIGN.md` → "Redesign in progress" (heading kept for history; the section itself documents the completed decisions) has the full token mapping and every conflict-and-resolution against the pre-redesign system; "Phase 7 — Motion pass" and "Phase 8 — Quality pass and delivery" close it out. **Everything in this file's Colors/Typography/Components sections below now describes the superseded pre-redesign system** — kept as historical reference, not current fact. Where a bullet further down in this section (Case-study header, Homepage "About", Engineering Projects rows, dark mode, skills grid) has been updated to reflect what actually shipped, it says so; anything not called out below (including by the 2026-09-05 paragraph above) should be re-verified against the current code rather than assumed. `.claude/skills` are unaffected by either redesign. `git diff` between `4924f85` and its parent `40b55c5` is the full, authoritative before/after for this first pass if a bullet here is unclear.

- `docs/PRD_Portfolio_Badr_Eddine_ELBOUAMRI.md` — the full product spec. It is the source of truth for design tokens, content structure, and copy rules. Read the relevant section before building the related feature rather than relying on memory of it.
- `docs/tasks.md` — milestone checklist (M0–M7) derived from the PRD's roadmap (§13). Reflects actual progress; check items off as they're completed.

Build/lint commands: `npm run dev` (Turbopack dev server), `npm run build` (production build — also validates all MDX case study frontmatter against the Zod schema in `lib/schema.ts`, so a bad frontmatter field fails the build, not just lint), `npm run lint` (ESLint), `npm run start` (serve a production build). There is no test suite yet.

As of M7, all 5 case study categories (`methodes`, `lean`, `maintenance`, `conception`, `digital`) have at least one published case study — the `conception` gap flagged throughout M2–M6 (homepage's 3rd featured slot, the Compétences page's "Conception & Simulation" and "Sciences de l'ingénieur" evidence lines) is closed. `docs/` also holds source material for case studies (PDFs, PPTX/DOCX reports, MP4s) that is not itself shipped — only the content extracted from it into `content/{fr,en}/projets/*.mdx` and `public/` is.

## What this project is

A bilingual (FR/EN) portfolio site for a mechanical engineer targeting Moroccan industrial/automotive employers. It is a static content site, not an app with users/auth/database. The two audiences with opposing needs — HR screening in 30 seconds vs. an engineering manager reading a case study in detail — is the central design constraint driving nearly every layout decision (PRD §2, §4).

## Planned architecture (PRD §9)

- **Stack:** Next.js 15 App Router + TypeScript strict, Tailwind CSS v4 (tokens in `@theme`), next-intl for locale routing (`/fr` default, `/en`), MDX content validated with Zod at build time, Recharts for charts, `@react-three/fiber` dynamically imported for the optional 3D viewer, Resend for the contact form, Vercel Web Analytics. **No database, no auth in V1** — content lives in-repo as MDX, git is the CMS.
- **Content is the bottleneck, not code** (PRD §10) — most milestones are blocked on writing/translating case studies before the corresponding page can be wired up with real content.
- **Case studies are the core content unit.** Every one follows a fixed 12-block skeleton (PRD §6) and a Zod-validated frontmatter schema (category, kpis, tools, confidential flag, etc.). The skeleton order is intentional and must not be reordered per-project — that consistency is what makes the site skimmable.
- **Confidentiality is load-bearing, not cosmetic.** Case studies drawn from internship work (Stellantis, AIC, Nexteer) must never publish absolute costs, volumes, real part/supplier names, or screenshots of internal documents/dashboards — see the hard rules in PRD §6. Any Stellantis-sourced content additionally requires verifying the confidentiality clause in the convention de stage first (M0). When touching content files, check the `confidential` frontmatter field and the organisation's `organisationPublic` flag before rendering real names.
- **Design tokens are centrally enforced**, not ad hoc — colors, type scale, and spacing are declared as CSS variables per PRD §8.2–8.4 specifically to prevent an AI-assisted build from drifting into generic/templated styling. Reuse the tokens rather than introducing new colors or one-off spacing values. As of the 2026-09-02 redesign, the 9 core tokens carry the blue-night/brass palette (Bodoni Moda display + Inter Tight body, self-hosted; brass `--accent` replacing petrol) rather than the values the PRD and this file's own Colors/Typography sections describe — see DESIGN.md → "Retained tokens — same 8 names, reskinned per theme" for the authoritative current values in both themes.
- **Case-study header — `CaseHero.tsx` deleted, rebuilt inline** (redesign Phase 6, 2026-09-02) — the standalone `CaseHero` component (full-width image only, no metadata, itself a 2026-09-01 replacement of the original Cartouche field-grid) is gone. `app/[locale]/projets/[slug]/page.tsx` now renders the header directly: a category filet (`SectionLabel`), a serif `<h1>`, and a monospace meta line (`période → période · organisation · rôle`, organisation falling back to a generic label when `organisationPublic` is false) — reintroducing the metadata the 2026-09-01 change had dropped, per the brief's explicit `/projets/[slug]` spec. Below that, case studies with a real photo (`hero` frontmatter) show it in a `FramedImage` (offset hairline frame, 21:9); the rest — mostly the confidentiality-restricted internship case studies, which can never carry a real photo per the confidentiality rules below — fall back to the same `.hero-grid-bg` technical-grid pattern used on the homepage hero, so an empty header still reads as deliberate rather than broken. Net effect versus the original PRD §8.6 Cartouche: the field-grid layout is gone for good, but the fields themselves are back, just as a single mono line instead of a grid.
- **Homepage "About" section — demoted to one paragraph** (redesign Phase 4, 2026-09-02) — the 5-paragraph `about_eyebrow`/`about_title`/`about_p1`–`about_p5` narrative (itself a 2026-09-01 replacement of the old KPI "Preuves" stat bar) no longer appears in full on the homepage. `app/[locale]/page.tsx`'s Bloc 3 ("Note de positionnement") now shows just `about_p1` — reused as-is, not rewritten — as a single lead paragraph in a 7-of-12 offset column, plus the `contact_mobility` availability line; this is the brief's §4.1 spec for that slot, not a content trim. The full 5-paragraph narrative is still published, on `/parcours` (`app/[locale]/parcours/page.tsx`), but sourced differently there: `content/{fr,en}/biographie.md`, read at build time via `readBiographyParagraphs()`, not the `about_p1`–`p5` i18n keys — those keys now back only the homepage's single paragraph. Gotcha (still applies wherever `t.rich` wraps inline emphasis): next-intl's ICU parser treats an apostrophe immediately before a `<b>` tag as a literal-text escape that swallows the tag — write `<b>l'ENSET Mohammedia</b>` (apostrophe inside the tag), never `l'<b>ENSET Mohammedia</b>`.
- **Engineering Projects rows — `ProjectRow.tsx` deleted, one-off exception fully retired** (redesign Phase 5/6, 2026-09-02; `.font-editorial` exception itself removed 2026-09-05) — the 2026-09-01 `ProjectRow` (dark, editorial image+text rows cloned from a reference site) is gone. The homepage's featured section (`FeaturedProjects`) and `/projets` (`ProjectsExplorer`) both now render the shared `ProjectCard` (`components/case/ProjectCard.tsx`, new — not the identically-named pre-2026-09-01 component, which is also gone), a token-system card (uses `Card`/`FramedImage`, hover-scale gated to `@media (hover: hover)`). `ProjectsHeroBand.tsx`'s `<h1>` used to set `.font-editorial` (Georgia/Times New Roman serif) as a deliberate, scoped exception for the `/projets` hero title — as of the 2026-09-05 redesign that's gone too: the component was simplified to a plain centered heading + short accent rule (no stock photo band), rendered in the sitewide display face (Archivo Black) like every other heading, and `.font-editorial` was deleted from `app/globals.css`. No component anywhere sets a serif face now. `.text-editorial-accent` (`#b5342c` red) was dead CSS with no component usage — removed 2026-09-02 (`app/globals.css` and its two `.impeccable/config.json` `ignore-value` entries).
- **Compétences page skills matrix (`SkillsToolbox`, `components/skills/SkillsToolbox.tsx`)** — as of 2026-09-01, replaces the old two-column `SkillsMatrix` (deleted) with a filterable "toolbox": rounded category tabs (`aria-pressed`, horizontally scrollable below 640px via an explicit `min-[640px]:` breakpoint — the project's `--breakpoint-sm` is 768px, not Tailwind's default 640px, so don't reach for `sm:` here) + a wrapping pill grid (icon or 2-letter monogram fallback, name, 3-dot level indicator). `lib/skills-data.ts` remains the single source of truth: each `Skill` gained `icon?: string` (filename in `/public/icons/skills/`, displayed at 20px, with a graceful `onError` monogram fallback if the file is missing) and `toolbox?: boolean` (default true; `false` on the 4 engineering-science skills — machines thermiques, transfert de chaleur, mécanique des fluides, énergies renouvelables — which don't fit the toolbox's tool/method categories and are only shown in "Preuves par domaine"'s sciences group). Clicking a category tab filters client-side and surfaces a "Voir les preuves →"/"See the evidence →" link that smooth-scrolls to the matching `id="preuve-<key>"` anchor added to each Preuves-par-domaine group (`cao`+`simulation` both point at `preuve-conception`, since that evidence block bundles CAD and simulation tools together). The rounded "pill" shape needed a new `--radius-pill` token (`app/globals.css`) — the only radius exception sitewide (everything else is `--radius-hairline`/2px or square) — logged in `.impeccable/config.json`, scoped to `.skill-pill`/toolbox filter buttons only. Icons: the owner supplied 7 vendor logos in `docs/img/` (not shipped — source material only), each cropped/downscaled to a 128px square PNG in `/public/icons/skills/` — `catia.png`, `solidworks.png`, `autocad.png` (cropped from a wordmark: the source asset paired a square "A" cube mark with "AutoCAD" text, isolated via canvas pixel-boundary detection since no image-editing CLI was available in this environment), `power-bi.png`, `excel.png`, `python.png`, `matlab.png`. ANSYS and ABAQUS have no `icon` set — the owner's source assets for both were wordmark-only logos with no isolable square mark, so both intentionally fall back to the monogram, same as every skill with no real-world logo. The redesign (Phase 6, 2026-09-02) gave `SkillsToolbox` a visual pass only — new tokens/type/hairline styling — with no logic change; the checkpoint explicitly kept this component and the 6-group Preuves-par-domaine structure as-is rather than adopting the brief's own per-skill-evidence layout (see DESIGN.md → "Retained tokens" → "Resolved since").
- **Nav/section labels — "Engineering Projects" naming unchanged, but the homepage no longer uses the big serif variant** — `Header.nav_projets` and `Projets.title` (both locales) are still "Projets d'ingénierie"/"Engineering Projects" (2026-09-01 rename, untouched by the redesign). What changed (redesign Phase 4, 2026-09-02): the homepage's featured-projects heading now reads `Home.featured_eyebrow` ("Projets en vedette"/"Featured projects") through the small mono `SectionLabel` component, the same eyebrow convention every other homepage section uses — not the large `.font-editorial` serif heading this bullet used to describe. `Home.projets_eyebrow` (the old value, "Projets d'ingénierie") was an orphaned key with no code reference — removed from `messages/{fr,en}.json` 2026-09-02.
- **Dark mode — superseded, the site is now dark-only** (2026-09-05 redesign). `ThemeToggle.tsx` is **deleted**; there is no `data-theme` attribute, no `localStorage("theme")`, and no `@media (prefers-color-scheme: dark)` branch — `:root` in `app/globals.css` carries the dark values directly. The paragraphs below describe the 2026-09-02 light/dark system this replaced, kept for history: token values replaced by the redesign's blue-night/brass palette (redesign Phase 1, 2026-09-02, superseding the 2026-09-02-earlier-same-day entry this bullet used to describe). Mechanism was: `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) {...} }` for OS-preference auto mode, `:root[data-theme="dark"]` for an explicit choice (wins over the media query), `ThemeToggle.tsx` (in `Header.tsx`) set `data-theme` + persisted to `localStorage("theme")`, no "reset to auto" control. The 9 tokens (`--paper`, `--surface`, `--surface-hover` *(new)*, `--ink`, `--graphite`, `--steel`, `--rule`, `--accent` *(brass, replaces petrol)*, `--signal`) all got new light **and** dark hex values, computed to clear 4.5:1 — see DESIGN.md → "Retained tokens — same 8 names, reskinned per theme" for that table and its contrast ratios (now itself superseded by DESIGN.md's 2026-09-05 token table). **Semantic swap, not just new hex:** `--signal` (blue) was halo/focus-ring only and was never used as text; every KPI/`<Stat>` headline figure that used to render `text-signal` rendered `text-accent` (brass) instead. The 2026-09-05 redesign keeps that same swap (KPI figures still render `text-accent`, now red) but repoints `--signal` to amber for its one remaining role — the two live demos' alert/error states — rather than blue/focus-only, since focus rings now use `--color-accent`. `OrgLogo.tsx`'s partner-logo cards still use a **fixed** light plate (`bg-white`, literal `#dce0e3`/`#626d77`, not theme tokens) since raster third-party logo art can't be auto-inverted safely — unaffected by either redesign. `.text-editorial-accent` and its dark-mode override are gone (removed 2026-09-02 — see the Engineering Projects rows bullet above).
- **Homepage "Ce que je sais faire" / "What I do" skills grid — `HomeSkillsGrid` superseded, not deleted** — the 2026-09-02 `HomeSkillsGrid` component (01–04 dark cards + center portrait) is no longer used: the redesign's Bloc 6 (`app/[locale]/page.tsx`) renders `CompetenceList` (`components/home/CompetenceList.tsx`, new — a hover-reveal-rule list format per `BRIEF-REFONTE-PORTFOLIO.md`) instead, built from the same `Home.competence_*` i18n keys with no content duplicated. `components/home/HomeSkillsGrid.tsx` and `public/images/profile-cutout.png` are still present on disk but **untracked** (never `git add`-ed, confirmed unused via `grep`) — dead code, not wired into any route; don't resurrect them without checking they still match the current token system first.
- **Demos are evidence, not nav destinations** — they live under `/fr/demos/[slug]`, linked from within case studies, never from the main header. They must never ship half-finished or with a visible loading/error state; a demo not yet ready stays behind a feature flag rather than shipping broken (PRD §11.2).
- Build order matters and is specified in PRD §9.5 — scaffold/tokens/i18n first, then layout shell, then the content pipeline + one seeded case study, then the case study template, then index/filtering, then homepage, then the remaining static pages, then contact, then SEO, then demos last.

## Conventions to follow when generating copy/content

- French is the source-of-truth locale; English is a reviewed translation, never machine-translated as-is (PRD §10.5 has known FR→EN term mappings for this domain, e.g. *gamme* → routing/process sheet, *rebut* → scrap).
- All numeric data (KPIs, dates, tool tags, figure captions) is set in monospace and French-formatted in FR (comma decimal, non-breaking space before `%`) — this is a deliberate typographic rule (PRD §8.3), not a default.
- No percentage/skill bars, no decorative sequence numbering on non-sequential content, no marketing adjectives without an evidence link — tone rules are in PRD §3.5 and §8.10.
## Important Note 
after major changes , please update this file (@CLAUDE.md). keep this file up-to-date with the project’s status.